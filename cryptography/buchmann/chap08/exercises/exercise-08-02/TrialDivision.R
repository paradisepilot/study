
TrialDivision <- function(n = NULL, list.of.primes = NULL) {
	
	LIST.output <- list(
		n              = n,
		list.of.primes = list.of.primes,
		prime.factors  = c(),
		exponents      = c(),
		unfactored     = 1
		);

	return(LIST.output);

	}

